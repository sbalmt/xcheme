/**
 * Context options.
 */
export type Options = {
  /**
   * Default context errors.
   */
  errors: {
    /**
     * Duplicate Symbol Identifier error code.
     */
    duplicateSymbolIdentifier: number;
  };
};
