// Exported directives.
export token <0> EXPORTED_TOKEN as 'token';

// This directive will be purged as a dependency of ALIAS_TOKEN1 during the importation,
// because it's not in use in the new context.
token <1> TOKEN as EXPORTED_TOKEN;
