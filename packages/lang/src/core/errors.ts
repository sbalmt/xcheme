import * as Core from '@xcheme/core';

/**
 * All errors in the language.
 */
export const enum Errors {
  DUPLICATE_IDENTIFIER = Core.Errors.DUPLICATE_IDENTIFIER,
  UNEXPECTED_TOKEN,
  UNEXPECTED_SYNTAX,
  UNEXPECTED_NODE,
  UNSUPPORTED_NODE,
  UNSUPPORTED_IDENTITY,
  UNDEFINED_IDENTIFIER,
  UNDEFINED_IDENTITY,
  UNDEFINED_AUTO_IDENTITY,
  UNRESOLVED_IDENTIFIER,
  UNRESOLVED_TOKEN_REFERENCE,
  UNRESOLVED_NODE_REFERENCE,
  UNRESOLVED_ALIAS_TOKEN_REFERENCE,
  UNRESOLVED_ALIAS_NODE_REFERENCE,
  INVALID_TOKEN_REFERENCE,
  INVALID_NODE_REFERENCE,
  INVALID_ALIAS_TOKEN_REFERENCE,
  INVALID_ALIAS_NODE_REFERENCE,
  INVALID_MAP_REFERENCE,
  INVALID_MAP_ENTRY_REFERENCE,
  INVALID_MAP_ENTRY,
  INVALID_EXPORT,
  TOKEN_COLLISION,
  IMPORT_DISABLED,
  IMPORT_NOT_FOUND,
  IMPORT_FAILURE,
  IMPORT_CYCLIC
}
